<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#C67C4E">
    <title>Checkout - Bean & Brew</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/checkout.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <meta name="description" content="Complete your coffee order securely with Bean & Brew.">
    <style>
        /* Modern Toast Notification Styles - Fixed Bottom Right */
        .toast-container {
            position: fixed !important;
            bottom: 20px !important;
            right: 20px !important;
            z-index: 9999 !important;
            pointer-events: none;
        }

        .toast-container .toast {
            pointer-events: auto;
            min-width: 320px;
            max-width: 400px;
            margin-bottom: 12px;
            border-radius: 12px;
            overflow: hidden;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12), 0 4px 8px rgba(0, 0, 0, 0.08);
            border: none;
            animation: slideInRight 0.3s ease-out;
        }

        @keyframes slideInRight {
            from {
                transform: translateX(400px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .toast.hiding {
            animation: slideOutRight 0.3s ease-in;
        }

        @keyframes slideOutRight {
            from {
                transform: translateX(0);
                opacity: 1;
            }
            to {
                transform: translateX(400px);
                opacity: 0;
            }
        }

        .toast-header {
            padding: 14px 16px;
            font-weight: 600;
            font-size: 15px;
            border-bottom: none !important;
        }

        .toast-header .btn-close {
            padding: 0;
            margin-left: auto;
            opacity: 0.6;
            transition: opacity 0.2s ease, transform 0.2s ease;
        }

        .toast-header .btn-close:hover {
            opacity: 1;
            transform: scale(1.1);
        }

        .toast-body {
            padding: 14px 16px !important;
            font-size: 14px;
            line-height: 1.5;
            font-weight: 400;
        }

        .toast-header i {
            font-size: 18px;
            margin-right: 10px;
        }

        /* Responsive adjustments for mobile */
        @media (max-width: 768px) {
            .toast-container {
                bottom: 80px !important;
                right: 10px !important;
                left: 10px !important;
                width: auto !important;
            }

            .toast-container .toast {
                min-width: 280px;
                max-width: 100%;
                width: 100%;
            }
        }

        @media (max-width: 480px) {
            .toast-container {
                bottom: 70px !important;
                right: 8px !important;
                left: 8px !important;
            }

            .toast-container .toast {
                min-width: 100%;
                border-radius: 10px;
            }

            .toast-header {
                padding: 12px 14px;
                font-size: 14px;
            }

            .toast-body {
                padding: 12px 14px !important;
                font-size: 13px;
            }

            .toast-header i {
                font-size: 16px;
            }
        }

        /* Ensure toast stays above other elements */
        .toast-container * {
            box-sizing: border-box;
        }

        /* Modern color adjustments for better visibility */
        .toast {
            background: rgba(255, 255, 255, 0.98) !important;
        }

        /* Success Toast - Modern Green */
        .toast[style*="background-color: rgb(212, 237, 218)"] {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%) !important;
        }

        /* Error Toast - Modern Red */
        .toast[style*="background-color: rgb(248, 215, 218)"] {
            background: linear-gradient(135deg, #f8d7da 0%, #f5c6cb 100%) !important;
        }

        /* Warning Toast - Modern Yellow */
        .toast[style*="background-color: rgb(255, 243, 205)"] {
            background: linear-gradient(135deg, #fff3cd 0%, #ffeaa7 100%) !important;
        }

        /* Info Toast - Modern Blue */
        .toast[style*="background-color: rgb(209, 236, 241)"] {
            background: linear-gradient(135deg, #d1ecf1 0%, #bee5eb 100%) !important;
        }

        /* Add subtle pulse animation on show */
        .toast.show {
            animation: slideInRight 0.3s ease-out, pulse 0.5s ease 0.3s;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.02);
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background Elements -->
    <div class="background-animation">
        <div class="coffee-bean bean-1"></div>
        <div class="coffee-bean bean-2"></div>
        <div class="coffee-bean bean-3"></div>
        <div class="coffee-bean bean-4"></div>
        <div class="coffee-bean bean-5"></div>
        <div class="steam steam-1"></div>
        <div class="steam steam-2"></div>
        <div class="steam steam-3"></div>
    </div>

    <%- include('../partials/navbar', { page: 'checkout' }) %>

    <main class="checkout-page">
        <div class="container">
            <!-- Header with Progress -->
            <header class="checkout-header">
                <h1><i class="fas fa-shopping-bag"></i> Secure Checkout</h1>
                <p class="checkout-subtitle">Complete your order in a few simple steps</p>
                
                <div class="progress-indicator">
                    <div class="progress-step completed">
                        <div class="step-icon"><i class="fas fa-cart-shopping"></i></div>
                        <span class="step-label">Cart</span>
                    </div>
                    <div class="progress-line completed"></div>
                    <div class="progress-step active">
                        <div class="step-icon"><i class="fas fa-clipboard-list"></i></div>
                        <span class="step-label">Checkout</span>
                    </div>
                    <div class="progress-line"></div>
                    <div class="progress-step">
                        <div class="step-icon"><i class="fas fa-credit-card"></i></div>
                        <span class="step-label">Payment</span>
                    </div>
                    <div class="progress-line"></div>
                    <div class="progress-step">
                        <div class="step-icon"><i class="fas fa-circle-check"></i></div>
                        <span class="step-label">Complete</span>
                    </div>
                </div>
            </header>

            <div class="checkout-grid">
                <!-- Left: Form -->
                <section class="checkout-form">
                    <!-- Contact Information -->
                    <div class="form-section">
                        <div class="section-header">
                            <h2><i class="fas fa-user"></i> Contact Information</h2>
                            <div class="contact-action-buttons">
                                <button type="button" class="edit-contact-btn" id="editContactBtn" title="Edit contact information">
                                    <i class="fas fa-edit"></i> Edit
                                </button>
                                <button type="button" class="cancel-contact-btn" id="cancelContactBtn" title="Cancel editing" style="display: none;">
                                    <i class="fas fa-times"></i> Cancel
                                </button>
                            </div>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <label for="fullName">Full Name <span class="required">*</span></label>
                                <input type="text" id="fullName" placeholder="Juan Dela Cruz" value="<%= user.fullName %>" required readonly>
                            </div>
                        </div>
                        <div class="form-row two-col">
                            <div class="form-group">
                                <label for="email">Email <span class="required">*</span></label>
                                <input type="email" id="email" placeholder="juan@example.com" value="<%= user.email %>" required readonly>
                            </div>
                            <div class="form-group">
                                <label for="phone">Phone Number <span class="required">*</span></label>
                                <input type="tel" id="phone" placeholder="+63 912 345 6789" value="<%= user.phone %>" required readonly>
                            </div>
                        </div>
                        <div class="contact-info-note" style="display: none;">
                            <i class="fas fa-info-circle"></i> 
                            <span>Changes here will only apply to this order and won't update your account profile.</span>
                        </div>
                    </div>

                    <!-- Choose Branch -->
                    <div class="form-section">
                        <div class="section-header">
                            <h2><i class="fas fa-store"></i> Choose Pickup Branch</h2>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <label for="branch">Select Branch <span class="required">*</span></label>
                                <select id="branch" class="branch-select" required>
                                    <option value="">-- Select a branch --</option>
                                    <!-- Branches will be loaded dynamically -->
                                </select>
                                <div class="branch-info" id="branchInfo" style="display: none;">
                                    <i class="fas fa-location-dot"></i>
                                    <div class="branch-details">
                                        <strong id="branchName"></strong>
                                        <small id="branchAddress"></small>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <label for="notes">Pickup Notes (Optional)</label>
                                <textarea id="notes" rows="3" placeholder="e.g. Preferred pickup time, special instructions"></textarea>
                            </div>
                        </div>
                    </div>
                </section>

                <!-- Right: Order Summary -->
                <aside class="order-summary-sticky">
                    <div class="order-summary checkout-card">
                        <h2><i class="fas fa-receipt"></i> Order Summary</h2>
                        
                        <div class="order-items" id="orderItems">
                            <!-- Items will be loaded from cart -->
                        </div>

                        <div class="order-totals">
                            <div class="total-row"><span>Subtotal</span><span id="checkoutSubtotal">₱0.00</span></div>
                            <div class="total-row"><span>Tax (2%)</span><span id="checkoutShipping">₱0.00</span></div>
                            <div class="total-row" style="display:none;"><span>Tax</span><span id="checkoutTax">₱0.00</span></div>
                            <div class="total-row discount-row" id="checkoutDiscountRow" style="display:none">
                                <span>Discount</span><span id="checkoutDiscount">–₱0.00</span>
                            </div>
                        </div>

                        <div class="total-main">
                            <span>Total</span>
                            <span id="checkoutTotal">₱0.00</span>
                        </div>

                        <button class="place-order-btn" id="placeOrderBtn">
                            <i class="fas fa-lock"></i> Continue to Payment
                        </button>

                        <div class="security-badges">
                            <div class="badge-item"><i class="fas fa-shield-halved"></i> Secure Payment</div>
                            <div class="badge-item"><i class="fas fa-rotate-left"></i> Easy Returns</div>
                        </div>
                    </div>
                </aside>
            </div>
        </div>
    </main>

    <!-- Back to Top Button -->
    <button class="back-to-top" id="backToTop" onclick="scrollToTop()" aria-label="Back to top">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M12 19V5M5 12L12 5L19 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
    </button>

    <!-- Bottom Navigation (Mobile) -->
    <%- include('../partials/generalBottomNav', { page: 'checkout' }) %>

    <!-- Bootstrap JS for Toast -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Checkout JavaScript -->
    <script src="/js/user/checkout.js"></script>
</body>
</html>
