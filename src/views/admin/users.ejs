<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management - Bean & Brew Admin</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>

    <!-- Sidebar Overlay -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <!-- Sidebar -->
    <%- include('../partials/adminSidebar', { page: 'admin-users' }) %>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <%- include('../partials/adminHeader') %>

        <!-- Page Content -->
        <div class="page-content">
            <div class="page-header">
                <h1>User Management</h1>
                <p>Manage user accounts, roles, and permissions</p>
            </div>

            <!-- Users Table -->
            <div class="data-table-container">
                <div class="filters-bar">
                    <div class="filter-search">
                        <i class="fas fa-search"></i>
                        <input type="text" placeholder="Search users by name or email...">
                    </div>
                </div>

                <table class="data-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Users will be loaded dynamically via JavaScript -->
                        <tr>
                            <td colspan="5" style="text-align: center; padding: 40px;">
                                <i class="fas fa-spinner fa-spin" style="font-size: 32px; color: var(--brew-primary);"></i>
                                <p style="color: var(--brew-text-light); margin-top: 12px;">Loading users...</p>
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="pagination">
                    <div class="pagination-info">Showing 0-0 of 0 users</div>
                    <div class="pagination-buttons">
                        <button class="pagination-btn" disabled>&laquo;</button>
                        <button class="pagination-btn active">1</button>
                        <button class="pagination-btn" disabled>&raquo;</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Suspend User Modal -->
    <div class="modal fade" id="suspendUserModal" tabindex="-1" aria-labelledby="suspendUserModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="suspendUserModalLabel">
                        <i class="fas fa-ban"></i> Suspend User
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="suspendUserForm">
                        <input type="hidden" id="suspendUserId" name="userId">
                        
                        <div class="mb-3">
                            <label class="form-label fw-bold">User Email:</label>
                            <p id="suspendUserEmail" class="text-muted mb-0"></p>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="suspensionType" class="form-label fw-bold">Suspension Type</label>
                                <select class="form-select" id="suspensionType" name="suspensionType" required>
                                    <option value="permanent">Permanent Suspension</option>
                                    <option value="temporary">Temporary Suspension</option>
                                </select>
                            </div>

                            <div class="col-md-6 mb-3" id="suspensionDateGroup" style="display: none;">
                                <label for="suspensionEndDate" class="form-label fw-bold">Suspension End Date</label>
                                <input type="datetime-local" class="form-control" id="suspensionEndDate" name="suspensionEndDate">
                                <small class="text-muted">Required for temporary suspension</small>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="suspensionReason" class="form-label fw-bold">Reason for Suspension <span class="text-danger">*</span></label>
                            <textarea class="form-control" id="suspensionReason" name="reason" rows="3" required 
                                placeholder="Enter the reason for suspending this user..."></textarea>
                        </div>

                        <div class="mb-3">
                            <label for="emailTitle" class="form-label fw-bold">Email Subject <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="emailTitle" name="emailTitle" 
                                value="Account Suspension Notice - Bean & Brew" required>
                        </div>

                        <div class="mb-3">
                            <label for="emailMessage" class="form-label fw-bold">Additional Email Message</label>
                            <textarea class="form-control" id="emailMessage" name="emailMessage" rows="3" 
                                placeholder="Optional: Additional message to include in the suspension email..."></textarea>
                        </div>

                        <div class="alert alert-warning mb-0" role="alert">
                            <i class="fas fa-exclamation-triangle"></i>
                            <strong>Warning:</strong> The user will receive an email notification and will be unable to access their account.
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Cancel
                    </button>
                    <button type="button" class="btn btn-danger" id="btnSuspendUser" onclick="confirmSuspendUser()">
                        <i class="fas fa-ban"></i> Suspend User
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Lift Suspension Modal -->
    <div class="modal fade" id="liftSuspensionModal" tabindex="-1" aria-labelledby="liftSuspensionModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="liftSuspensionModalLabel">
                        <i class="fas fa-check-circle"></i> Lift Suspension
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="liftUserId">
                    <p>Are you sure you want to lift the suspension for this user?</p>
                    <p class="fw-bold" id="liftUserEmail"></p>
                    <div class="alert alert-info mb-0" role="alert">
                        <i class="fas fa-info-circle"></i>
                        The user will receive an email notification and will be able to access their account again.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Cancel
                    </button>
                    <button type="button" class="btn btn-success" id="btnLiftSuspension" onclick="confirmLiftSuspension()">
                        <i class="fas fa-check"></i> Lift Suspension
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Delete User Modal -->
    <div class="modal fade" id="deleteUserModal" tabindex="-1" aria-labelledby="deleteUserModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="deleteUserModalLabel">
                        <i class="fas fa-trash-alt"></i> Delete User Account
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="deleteUserId">
                    <div class="text-center mb-3">
                        <i class="fas fa-exclamation-triangle text-warning" style="font-size: 3rem;"></i>
                    </div>
                    <h6 class="text-center mb-3">Are you sure you want to delete this user account?</h6>
                    <p class="text-center fw-bold mb-3" id="deleteUserEmail"></p>
                    <div class="alert alert-danger mb-0" role="alert">
                        <i class="fas fa-exclamation-circle"></i>
                        <strong>Warning:</strong> This action cannot be undone. All user data will be permanently deleted.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Cancel
                    </button>
                    <button type="button" class="btn btn-danger" id="btnDeleteUser" onclick="confirmDeleteUser()">
                        <i class="fas fa-trash-alt"></i> Delete Account
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin/users.js"></script>
</body>
</html>
