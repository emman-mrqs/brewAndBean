<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Management - Bean & Brew Admin</title>
    <!-- Bootstrap for responsive modal/ui -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
            /* small overrides: keep product thumbnail sizing */
            .product-thumb{ width:48px; height:48px; border-radius:8px; object-fit:cover }
            /* adapt table cells to align thumbnails */
            .product-cell { display:flex; align-items:center; gap:12px }

            /* We'll use Bootstrap's .table-responsive wrapper for overflow handling */

            /* Keep table full width but don't force each cell to 100% which breaks layout */
            .data-table { width: 100%; table-layout: auto; }
            .data-table th, .data-table td { white-space: nowrap; }

            /* Page-local tweak: reduce cell padding for tighter rows on this page only */
            .table-responsive .data-table th,
            .table-responsive .data-table td { padding: 10px 14px; }

            /* Optional: make sure small tables still look good by allowing them to shrink */
            @media (max-width: 576px) {
                .data-table { min-width: 560px; }
            }

            /* Modal sizing for delete confirmation dialog */
            @media (min-width: 992px) {
                #productDeleteModal .modal-dialog { max-width: 680px; }
            }
            @media (max-width: 991.98px) {
                /* keep dialog margins small on tablet and below */
                #productDeleteModal .modal-dialog { margin: 1.75rem; }
            }
            /* On small screens ensure the delete modal is centered and scrolls if needed */
            @media (max-width: 767.98px) {
                #productDeleteModal.modal {
                    display: flex !important;
                    align-items: center !important;
                    justify-content: center !important;
                    padding: 1rem;
                }
                #productDeleteModal .modal-dialog {
                    max-width: calc(100% - 2rem);
                    margin: 0;
                }
                #productDeleteModal .modal-content {
                    max-height: calc(100vh - 3rem);
                    overflow: auto;
                }
            }

            /* Fix for mobile freezing issue - ensure modals don't block interaction */
            @media (max-width: 768px) {
                /* Prevent body scroll lock from persisting */
                body:not(.modal-open) {
                    overflow: auto !important;
                    padding-right: 0 !important;
                }

                /* Ensure modal backdrops are properly layered */
                .modal-backdrop {
                    z-index: 1040;
                }

                .modal {
                    z-index: 1050;
                }

                /* Ensure buttons remain clickable on mobile */
                .btn-icon, .btn-edit, .btn-delete, .btn-primary, .btn-secondary {
                    touch-action: manipulation;
                    -webkit-tap-highlight-color: rgba(0,0,0,0.1);
                }

                /* Prevent accidental double-tap zoom on buttons */
                button {
                    touch-action: manipulation;
                }

                /* Ensure table doesn't interfere with modals */
                .data-table-container {
                    position: relative;
                    z-index: 1;
                }

                /* Make sure pagination controls are accessible */
                .pagination {
                    position: relative;
                    z-index: 2;
                }
            }
    </style>
</head>
<body>
    <!-- Sidebar Overlay -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <!-- Sidebar -->
    <%- include('../partials/adminSidebar', { page: 'admin-products' }) %>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <%- include('../partials/adminHeader') %>

        <!-- Page Content -->
        <div class="page-content">
            <div class="page-header">
                <h1>Product Management</h1>
                <p>Manage your coffee products, equipment, and accessories</p>
            </div>

            <!-- Products Table -->
            <div class="data-table-container">
                <div class="filters-bar">
                    <div class="filter-search">
                        <i class="fas fa-search"></i>
                        <input id="productSearch" type="text" placeholder="Search products by name, category, or ID...">
                    </div>
                    <button class="btn btn-primary">
                        <i class="fas fa-plus"></i> Add New Product
                    </button>
                </div>

                <div class="table-responsive">
                <table class="table data-table">
                    <thead>
                        <tr>
                            <th>Product ID</th>
                            <th>Name</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Stock</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Rows are rendered dynamically by /public/js/admin/products.js -->
                    </tbody>
                </table>
                </div>

                <div id="productsPagination" class="pagination"></div>
            </div>
        </div>
    </div>

    <!-- Server-rendered Add Product modal (restored) - Edit is inline now -->
    <div class="modal fade" id="productModal" tabindex="-1">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="productModalTitle">Add Product</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="productForm" enctype="multipart/form-data">
                        <input type="hidden" name="id" id="productFormId" />
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label">Name</label>
                                <input name="name" id="productFormName" required class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Price</label>
                                <input name="price" id="productFormPrice" type="number" step="0.01" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                    <label class="form-label">Category (variant name)</label>
                                    <!-- fixed category options: hot, cold, special -->
                                    <select id="productFormVariant" class="form-select" name="variant_name">
                                        <option value="">Select category</option>
                                        <option value="hot">hot</option>
                                        <option value="cold">cold</option>
                                        <option value="special">special</option>
                                    </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Quantity</label>
                                <input name="stock_quantity" id="productFormStock" type="number" min="0" class="form-control" />
                            </div>
                            <div class="col-12">
                                <label class="form-label">Description</label>
                                <textarea name="description" id="productFormDescription" rows="4" class="form-control"></textarea>
                            </div>
                            <div class="col-12 d-flex align-items-center gap-3">
                                <div>
                                    <label class="form-label">Image</label>
                                    <input name="image" id="productFormImage" type="file" accept="image/*" class="form-control" />
                                </div>
                                <div id="productFormPreview"></div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" id="productFormCancel" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" id="productFormSubmit" class="btn btn-primary">Create product</button>
                </div>
            </div>
        </div>
    </div>

        <!-- Delete confirmation modal -->
        <div class="modal fade" id="productDeleteModal" tabindex="-1">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">Delete product</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <p id="productDeleteMessage">Are you sure you want to delete this product?</p>
                        <input type="hidden" id="productDeleteId" />
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" id="productDeleteConfirm" class="btn btn-danger">Delete</button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Toasts container (lower-right) -->
        <div id="toastContainer" class="position-fixed bottom-0 end-0 p-3" style="z-index: 2100;"></div>

    <!-- Bootstrap bundle (Popper included) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin/products.js"></script>
</body>
</html>
